// Design tokens for use in JavaScript contexts (e.g. Mapbox paint properties)
// that can't read CSS variables directly.
// Keep these in sync with the CSS variables in app/globals.css.

export const colors = {
  primary: "#5EC26A", // --primary
  primaryMuted: "#aed0b8", // --primary-muted: oklch(0.84 0.06 148)
}
