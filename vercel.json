{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ]; then git diff --quiet HEAD^ HEAD -- . ':(exclude)data/*.json'; else exit 1; fi"
}
